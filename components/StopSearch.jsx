import styles from "../styles/layouts/stopsearch.module.scss"

const StopSearch = () => {
      return (
            <section className={styles.stopsearch}>
                  <h1>Stop right now thank you very much</h1>
            </section>
      )
}

export default StopSearch