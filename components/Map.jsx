import styles from "../styles/components/map.module.scss"

const Map = () => {
      return (
            <div className={styles.map}>
                  Map
            </div>
      )
}

export default Map